services:
  postgres:
    image: postgres:latest
    container_name: postgres
    env_file:
      - ./.env
    ports:
      - "5432:5432"
    volumes:
    # this should preserve our data even if the container is removed
      - postgresDb:/var/lib/postgresql
volumes:
  postgresDb:
